# build artifacts
bin/
/oss-agent

# runtime data (cortexdb knowledge base + agent memory)
data/
*.db

# cloned source repos analyzed into the graph
repos/

# vendored external tooling (Understand-Anything: installed separately)
skills/

# misc
.DS_Store

# cross-compile output (make build-linux)
/dist/
